package handlers

import "github.com/fashionscape/fsbot-api/graph/model"

func CreateOutfit(input model.InputOutfit) (*model.Outfit, error) {
	// Add the outfit to the database, retrieve it, return it.
}
